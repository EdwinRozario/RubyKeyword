rubykeyword is a gem that lets you know if a string is a Ruby Keyword or not.

The gem has only two methodes as of now.
keyword and define

Use:

require 'rubygems'
require 'rubykeyword'

example_string1 = "Thor"
example_string2 = "end"

example_srting1.keyword? # Will return false since its not a ruby keyword
example_srting2.keyword? # Will return true as end is a ruby keyword
example_srting2.define # Will return definition for keyword end
exaple_string.example # Will print an example for the keyword use
exaple_string.example_string # Will return an example string

Data reference : http://ruby-doc.org/docs/keywords/1.9/
